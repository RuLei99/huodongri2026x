page { background: #f4f6fb; color: #143952; }

.page { min-height: 100vh; padding-bottom: calc(60rpx + env(safe-area-inset-bottom)); }

.hero { padding: 48rpx 32rpx; color: #fff; background: linear-gradient(145deg, #052a43, #245f86); }
.hero text { display: block; }
.hero text:first-child { color: #daa274; font-size: 18rpx; letter-spacing: 5rpx; }
.hero text:last-child { margin-top: 18rpx; font-size: 40rpx; font-weight: 800; }

.venue, .section {
  margin: 26rpx;
  padding: 30rpx;
  background: #fff;
  border: 1rpx solid rgba(192, 135, 89, 0.28);
  border-radius: 14rpx;
}

.venue text { display: block; }
.venue-label { color: #a9662f; font-size: 20rpx; letter-spacing: 3rpx; }
.venue-name { margin-top: 16rpx; font-size: 32rpx; font-weight: 800; }
.venue-address { margin-top: 12rpx; color: #858b97; font-size: 23rpx; line-height: 1.6; }

.venue-hall {
  display: inline-block;
  margin-top: 18rpx;
  padding: 10rpx 20rpx;
  color: #8d4f1d;
  font-size: 22rpx;
  font-weight: 700;
  background: rgba(217, 156, 106, 0.16);
  border: 1rpx solid rgba(171, 102, 46, 0.45);
  border-radius: 22rpx;
}

.venue button { margin-top: 24rpx; color: #fff; background: #1b567d; font-size: 24rpx; }

.title { font-size: 30rpx; font-weight: 800; }

.row {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  gap: 24rpx;
  padding: 24rpx 0;
  border-bottom: 1rpx solid #e4ebf0;
}

.row:last-child { border-bottom: none; padding-bottom: 0; }

.row.column { flex-direction: column; gap: 10rpx; }

.row-name { flex: none; font-size: 26rpx; font-weight: 700; }

.row-detail {
  flex: 1;
  color: #777f8d;
  font-size: 23rpx;
  line-height: 1.7;
  text-align: right;
}

.row.column .row-detail { text-align: left; }

.notice { margin: 34rpx 26rpx; color: #8c6545; font-size: 22rpx; text-align: center; line-height: 1.6; }
